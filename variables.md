# Variables

2 ways to declare varibles:

* val     (same as `const in javascript`)   (cannot be changed)
* var     (same as `var in javascript`)   (can be changed later)



```kotlin
fun main(){

    val name = "John cena";

    println(name);
}
```

